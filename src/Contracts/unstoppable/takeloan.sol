// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

import {UnstoppableLender} from "./UnstoppableLender.sol";

contract TakeLoan {
    address lender;
    // constructor(address Ilender) {
    //     Unstoppable(lender) = Unstoppable(Ilender);
    // }

    // function TakeLoan(uint amount) public{

    // }
}
